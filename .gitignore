# --- Python (backend) ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend ---
node_modules/
frontend/dist/
.vite/
*.local

# --- DeskBot runtime artifacts ---
logs/
*.log
*.sqlite
*.sqlite3
*.db
deskbot.db*

# --- AI models (downloaded, not committed) ---
models/*
!models/.gitkeep

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# backend venv
backend/.venv/

# per-machine config override
config/local.yaml

# Google OAuth secrets (delivered to the Pi via rsync, never committed)
config/google/

# session signing secret (per-machine)
config/.session_secret
